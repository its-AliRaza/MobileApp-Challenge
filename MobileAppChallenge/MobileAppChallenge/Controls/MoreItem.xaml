<?xml version="1.0" encoding="UTF-8"?>
<Grid xmlns="http://xamarin.com/schemas/2014/forms" 
      xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
      xmlns:ffImageLoading="clr-namespace:FFImageLoading.Svg.Forms;assembly=FFImageLoading.Svg.Forms"
      x:Class="MobileAppChallenge.Controls.MoreItem"
      RowSpacing="0"
      Margin="10,5"
      CompressedLayout.IsHeadless="True">
    <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>
    <Frame Padding="15" BackgroundColor="White" CornerRadius="5" HasShadow="False">
        <StackLayout Orientation="Horizontal" >
            <Label x:Name="txt" Style="{StaticResource  SmallGrayMedium}" FontSize="Medium"/>
            <ffImageLoading:SvgCachedImage x:Name="imgArrow" Source="rightArrow.svg" Aspect="AspectFit" DownsampleToViewSize="True" HeightRequest="15" WidthRequest="15" VerticalOptions="Center" HorizontalOptions="EndAndExpand"/>
        </StackLayout>
    </Frame>
    <Button x:Name="btnView" Grid.Row="0" Style="{StaticResource TransparentButton}"/>
</Grid>